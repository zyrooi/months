if game.GameId == 7074860883 then
    local PremiumKeys = {
        "njzhh2qib8qjpbszvo7wao346p496mitpcm",
        "CAT-0IHEW-O7HFA-KHFSD-9829N-UYQDA",
    }

    local FreeKeys = {
        "Key_OXJwnm5AobmyIkAlgxIM2rFYLYEv8Y",
        "freeuser002",
        "guestkey123",
        "trialkey456"
    }

    local BlacklistKeys = {
        "gwg1wimqkoswlt1vs0numkufg4ciunj59yy",
        "F8vYkM5wT2QbL7cZ9rX6nP1h0A3dR4jz",
        "xyz789"
    }

    local function isKeyValid(list, key)
        for _, v in ipairs(list) do
            if v == key then
                return true
            end
        end
        return false
    end

    local userKey = getgenv().key

    if not userKey or isKeyValid(BlacklistKeys, userKey) then
        game.Players.LocalPlayer:Kick("[Switch-Cat-Hub]-Key Blacklist")
        return
    end

    if isKeyValid(PremiumKeys, userKey) then
        loadstring(game:HttpGet("https://raw.githubusercontent.com/zyrooi/months/refs/heads/main/Premium%20(1).txt"))()
    elseif isKeyValid(FreeKeys, userKey) then
        loadstring(game:HttpGet(""))()
    else
        game.Players.LocalPlayer:Kick("Invalid Key")
    end
else
    game.Players.LocalPlayer:Kick("[ Not Support ]")
end
